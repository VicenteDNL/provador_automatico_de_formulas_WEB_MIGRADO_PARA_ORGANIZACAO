export type Saude = {
  tentativas: {
    atual: number;
    inicial: number;
  };
  pontuacao: {
    atual: number;
    inicial: number;
  };
  tempo: {
    atual: number;
    inicial: number;
  };
};
