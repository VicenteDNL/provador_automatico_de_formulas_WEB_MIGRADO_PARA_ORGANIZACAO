export type Inicio = {
    completa: boolean;
    lista: [
        {
            idNo: string;
            negacao: false;
        },
        {
            idNo: string;
            negacao: boolean;
        }
    ];
    negacao: null;
    no: null;
    opcoes: [];
};
